[pytest]
testpaths = app/tests
pythonpath = app
markers =
    integration: invokes locally installed system tools such as pdflatex and pdfinfo
    browser: invokes locally installed Chromium against an isolated local server
